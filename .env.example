# ----------------------------------------------------
# MochiRAG Environment Variables
# ----------------------------------------------------
# Copy this file to .env and fill in the values.
# Do NOT commit the .env file to version control.

# --- Security (Required) ---
# A long, random string used for signing JWTs.
# You can generate one with: openssl rand -hex 32
SECRET_KEY="your-super-secret-and-long-string-for-jwt"

# --- LLM / Embedding Provider Configuration (Choose one or more) ---
# These are used by the LLMManager and EmbeddingManager if not specified in strategies.yaml
# The backend container needs to be able to reach these URLs.

# For a local Ollama instance running on the Docker host machine
# For Linux, this is often http://172.17.0.1:11434
# For Docker Desktop (Mac/Windows), it's often http://host.docker.internal:11434
OLLAMA_BASE_URL=http://172.17.0.1:11434

# For OpenAI API
OPENAI_API_KEY="sk-..."

# For Azure OpenAI
AZURE_OPENAI_API_KEY="..."
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"

# For Google Gemini
GOOGLE_API_KEY="AIzaSy..."


# --- ChromaDB Configuration (for docker-compose) ---
# These are used by the backend service inside docker-compose to find the chroma service.
# You generally don't need to change these for local development.
CHROMA_HOST=chroma
CHROMA_PORT=8000


# --- LangSmith Tracing (Optional, for debugging) ---
# Set to "true" to enable tracing
LANGCHAIN_TRACING_V2="true"
# Your LangSmith API key
LANGCHAIN_API_KEY="ls__..."
# A name for your project in LangSmith
LANGCHAIN_PROJECT="MochiRAG-Dev"
